/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { FranjaHorariaSemanalService } from './franjaHorariaSemanal.service';

describe('Service: FranjaHorariaSemanal', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [FranjaHorariaSemanalService]
    });
  });

  it('should ...', inject([FranjaHorariaSemanalService], (service: FranjaHorariaSemanalService) => {
    expect(service).toBeTruthy();
  }));
});
